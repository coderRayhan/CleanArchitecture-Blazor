//------------------------------------------------------------------------------
// <auto-generated>
//     This file is part of the CleanArchitecture.Blazor project.
//     Licensed to the .NET Foundation under the MIT license.
//     See the LICENSE file in the project root for more information.
//
//     Author: neozhu
//     Created Date: 2024-12-09
//     Last Modified: 2024-12-09
//     Description: 
//       Defines a specification for applying advanced filtering options to the 
//       MenuSection entity, supporting different views and keyword-based searches.
// </auto-generated>
//------------------------------------------------------------------------------

namespace CleanArchitecture.Blazor.Application.Features.MenuSections.Specifications;
#nullable disable warnings
/// <summary>
/// Specification class for advanced filtering of MenuSections.
/// </summary>
public class MenuSectionAdvancedSpecification : Specification<MenuSection>
{
    public MenuSectionAdvancedSpecification(MenuSectionAdvancedFilter filter)
    {
        DateTime today = DateTime.UtcNow;
        var todayrange = today.GetDateRange(MenuSectionListView.TODAY.ToString(), filter.LocalTimezoneOffset);
        var last30daysrange = today.GetDateRange(MenuSectionListView.LAST_30_DAYS.ToString(),filter.LocalTimezoneOffset);

        Query.Where(q => q.Title != null)
             .Where(filter.Keyword,!string.IsNullOrEmpty(filter.Keyword))
             .Where(q => q.CreatedBy == filter.CurrentUser.UserId, filter.ListView == MenuSectionListView.My && filter.CurrentUser is not null)
             .Where(x => x.Created >= todayrange.Start && x.Created < todayrange.End.AddDays(1), filter.ListView == MenuSectionListView.TODAY)
             .Where(x => x.Created >= last30daysrange.Start, filter.ListView == MenuSectionListView.LAST_30_DAYS);
       
    }
}
